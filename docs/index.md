# vella-core

Pure-data core model (`Node`, `Edge`, references, state, tools) for the Vella
graph SDK. Zero first-party dependencies — pydantic v2 and typing_extensions
only.

The full public surface is documented in the [API reference](api.md), generated
directly from the source docstrings and type hints.
